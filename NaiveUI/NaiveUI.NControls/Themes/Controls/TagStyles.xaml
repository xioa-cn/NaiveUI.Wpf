<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:NaiveUI.NControls.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/NaiveUI.NControls;component/Themes/ThemeTokens.xaml" />
        <ResourceDictionary Source="/NaiveUI.NControls;component/Themes/Controls/PathResources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="Naive.Tag.CloseButtonStyle" TargetType="Button">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Cursor" Value="Arrow" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Width" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:NTag}}, Path=ResolvedCloseSize}" />
        <Setter Property="Height" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:NTag}}, Path=ResolvedCloseSize}" />
        <Setter Property="Margin" Value="4,0,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="CloseBackground"
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        Background="Transparent"
                        CornerRadius="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:NTag}}, Path=ResolvedCloseCornerRadius}">
                        <controls:NIcon
                            x:Name="CloseIconElement"
                            Data="{StaticResource Close}"
                            Size="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:NTag}}, Path=ResolvedCloseIconSize}"
                            Color="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:NTag}}, Path=ResolvedCloseIconBrush}" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="CloseBackground" Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:NTag}}, Path=ResolvedCloseHoverBackground}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="CloseBackground" Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:NTag}}, Path=ResolvedClosePressedBackground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:NTag}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:NTag}">
                    <Border
                        x:Name="RootBorder"
                        MinHeight="{TemplateBinding ResolvedHeight}"
                        Padding="{TemplateBinding ResolvedPadding}"
                        Background="{TemplateBinding ResolvedBackground}"
                        BorderBrush="{TemplateBinding ResolvedBorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding ResolvedCornerRadius}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ContentPresenter
                                x:Name="AvatarPresenter"
                                Grid.Column="0"
                                Margin="{TemplateBinding ResolvedAvatarMargin}"
                                VerticalAlignment="Center"
                                Content="{Binding DisplayAvatarContent, RelativeSource={RelativeSource TemplatedParent}}"
                                TextElement.FontSize="{TemplateBinding ResolvedFontSize}"
                                TextElement.FontWeight="{TemplateBinding ResolvedFontWeight}"
                                TextElement.Foreground="{TemplateBinding Foreground}"
                                Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ShowAvatar, Converter={StaticResource BoolToVisibilityConverter}}" />

                            <ContentPresenter
                                x:Name="IconPresenter"
                                Grid.Column="1"
                                Margin="{TemplateBinding ResolvedIconMargin}"
                                VerticalAlignment="Center"
                                Content="{Binding DisplayIconContent, RelativeSource={RelativeSource TemplatedParent}}"
                                TextElement.FontSize="{TemplateBinding ResolvedFontSize}"
                                TextElement.FontWeight="{TemplateBinding ResolvedFontWeight}"
                                TextElement.Foreground="{TemplateBinding Foreground}"
                                Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ShowIcon, Converter={StaticResource BoolToVisibilityConverter}}" />

                            <ContentPresenter
                                Grid.Column="2"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Content}"
                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                RecognizesAccessKey="True"
                                TextElement.FontSize="{TemplateBinding ResolvedFontSize}"
                                TextElement.FontWeight="{TemplateBinding ResolvedFontWeight}"
                                TextElement.Foreground="{TemplateBinding Foreground}" />

                            <Button
                                x:Name="PART_CloseButton"
                                Grid.Column="3"
                                Style="{StaticResource Naive.Tag.CloseButtonStyle}"
                                Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ShowCloseButton, Converter={StaticResource BoolToVisibilityConverter}}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="ShowBorder" Value="False">
                            <Setter TargetName="RootBorder" Property="BorderThickness" Value="0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
