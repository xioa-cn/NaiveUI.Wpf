<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:NaiveUI.NControls.Controls">

    <Style TargetType="{x:Type controls:NWatermark}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="ClipToBounds" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:NWatermark}">
                    <Grid ClipToBounds="{TemplateBinding ClipToBounds}">
                        <ContentPresenter />

                        <Grid
                            x:Name="PART_OverlayHost"
                            Margin="{TemplateBinding ResolvedOverlayMargin}"
                            IsHitTestVisible="False"
                            Panel.ZIndex="{TemplateBinding ZIndex}">
                            <Grid.RenderTransform>
                                <RotateTransform Angle="{TemplateBinding GlobalRotate}" />
                            </Grid.RenderTransform>
                            <Grid.RenderTransformOrigin>0.5,0.5</Grid.RenderTransformOrigin>

                            <Rectangle Fill="{TemplateBinding ResolvedPrimaryBrush}" />
                            <Rectangle
                                x:Name="CrossOverlay"
                                Fill="{TemplateBinding ResolvedCrossBrush}"
                                Visibility="Collapsed" />
                        </Grid>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="ShowLocalOverlay" Value="False">
                            <Setter TargetName="PART_OverlayHost" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ShowCrossOverlay" Value="True">
                            <Setter TargetName="CrossOverlay" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
