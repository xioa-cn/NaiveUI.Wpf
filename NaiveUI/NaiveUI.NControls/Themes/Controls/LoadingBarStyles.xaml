<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:NaiveUI.NControls.Controls">
    <Style x:Key="NLoadingBarIndicatorBaseStyle" TargetType="{x:Type controls:NLoadingBarIndicator}">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="Opacity" Value="0" />
        <Setter Property="BarHeight" Value="2.5" />
        <Setter Property="NormalBrush" Value="{DynamicResource Primary.First.Brush}" />
        <Setter Property="ErrorBrush" Value="{DynamicResource Error.First.Brush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:NLoadingBarIndicator}">
                    <Grid Height="{TemplateBinding BarHeight}" ClipToBounds="False" IsHitTestVisible="False">
                        <Grid x:Name="PART_BarRoot" ClipToBounds="False">
                            <Grid.RenderTransform>
                                <ScaleTransform x:Name="PART_BarScale" ScaleX="0" ScaleY="1" CenterX="0" CenterY="0" />
                            </Grid.RenderTransform>
                            <Border
                                x:Name="BarElement"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Background="{TemplateBinding CurrentBrush}" />
                        </Grid>

                        <Border
                            x:Name="PegElement"
                            Width="36"
                            Height="{TemplateBinding BarHeight}"
                            HorizontalAlignment="Left"
                            Background="{TemplateBinding CurrentBrush}"
                            CornerRadius="0"
                            Opacity="0.45"
                            RenderTransformOrigin="0,0">
                            <Border.RenderTransform>
                                <TranslateTransform x:Name="PART_PegTranslate" X="0" />
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect
                                    BlurRadius="12"
                                    Opacity="0.45"
                                    ShadowDepth="0"
                                    Color="#FFFFFFFF" />
                            </Border.Effect>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:NLoadingBarIndicator}" BasedOn="{StaticResource NLoadingBarIndicatorBaseStyle}" />

    <Style x:Key="NLoadingBarProviderBaseStyle" TargetType="{x:Type controls:NLoadingBarProvider}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:NLoadingBarProvider}">
                    <Grid ClipToBounds="False">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentStringFormat="{TemplateBinding ContentStringFormat}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                            RecognizesAccessKey="True" />

                        <controls:NLoadingBarIndicator
                            x:Name="PART_LoadingBar"
                            BarHeight="{TemplateBinding BarHeight}"
                            Panel.ZIndex="4096"
                            Margin="0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Top" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:NLoadingBarProvider}" BasedOn="{StaticResource NLoadingBarProviderBaseStyle}" />
</ResourceDictionary>
