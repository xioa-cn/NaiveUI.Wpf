<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:NaiveUI.NControls.Controls">
    <!--  xmlns:loading="clr-namespace:NaiveUI.NControls.ControlsExample.Loading"  -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/NaiveUI.NControls;component/Themes/ThemeTokens.xaml" />

        <ResourceDictionary Source="/NaiveUI.NControls;component/Themes/Controls/PathResources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="Naive.Plain.IconButton" TargetType="Button">
        <Setter Property="Width" Value="22" />
        <Setter Property="Height" Value="22" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="Root"
                        Background="{TemplateBinding Background}"
                        CornerRadius="{StaticResource Radius.Round}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Theme.Fill.Hover.Brush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Theme.Fill.2.Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="NButtonBaseStyle" TargetType="{x:Type controls:NButton}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="Height" Value="34" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Padding" Value="14,0" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:NButton}">
                    <Grid>
                        <Border
                            x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding ResolvedCornerRadius}">
                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Viewbox
                                    x:Name="LoadingIndicator"
                                    Grid.Column="0"
                                    Width="14"
                                    Height="14"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Visibility="Collapsed">
                                    <Path
                                        Data="M 7,1 A 6,6 0 1 1 1,7"
                                        Stroke="{TemplateBinding ResolvedLoadingBrush}"
                                        StrokeEndLineCap="Round"
                                        StrokeStartLineCap="Round"
                                        StrokeThickness="2">
                                        <Path.RenderTransform>
                                            <RotateTransform x:Name="LoadingRotateTransform" CenterX="7" CenterY="7" />
                                        </Path.RenderTransform>
                                    </Path>
                                </Viewbox>

                                <ContentPresenter
                                    x:Name="ButtonContentPresenter"
                                    Grid.Column="1"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                    RecognizesAccessKey="True"
                                    TextElement.Foreground="{TemplateBinding Foreground}" />
                            </Grid>
                        </Border>

                        <Rectangle
                            x:Name="DashedBorder"
                            Margin="0.5"
                            RadiusX="{TemplateBinding ResolvedCornerRadiusValue}"
                            RadiusY="{TemplateBinding ResolvedCornerRadiusValue}"
                            Stroke="{TemplateBinding BorderBrush}"
                            StrokeDashArray="2 2"
                            StrokeThickness="1"
                            Visibility="Collapsed" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="Size" Value="Tiny">
                            <Setter Property="Height" Value="22" />
                            <Setter Property="FontSize" Value="12" />
                            <Setter Property="Padding" Value="8,0" />
                        </Trigger>
                        <Trigger Property="Size" Value="Small">
                            <Setter Property="Height" Value="28" />
                            <Setter Property="FontSize" Value="13" />
                            <Setter Property="Padding" Value="10,0" />
                        </Trigger>
                        <Trigger Property="Size" Value="Medium">
                            <Setter Property="Height" Value="34" />
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="Padding" Value="14,0" />
                        </Trigger>
                        <Trigger Property="Size" Value="Large">
                            <Setter Property="Height" Value="40" />
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="Padding" Value="18,0" />
                        </Trigger>
                        <Trigger Property="Kind" Value="Text">
                            <Setter Property="MinWidth" Value="0" />
                            <Setter TargetName="RootBorder" Property="BorderThickness" Value="0" />
                        </Trigger>
                        <Trigger Property="Kind" Value="Tertiary">
                            <Setter TargetName="RootBorder" Property="BorderThickness" Value="0" />
                        </Trigger>
                        <Trigger Property="Kind" Value="Quaternary">
                            <Setter Property="MinWidth" Value="0" />
                            <Setter TargetName="RootBorder" Property="BorderThickness" Value="0" />
                        </Trigger>
                        <Trigger Property="Kind" Value="Secondary">
                            <Setter TargetName="RootBorder" Property="BorderThickness" Value="0" />
                        </Trigger>
                        <Trigger Property="Kind" Value="Dashed">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="Transparent" />
                            <Setter TargetName="DashedBorder" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Margin" Value="0,1,0,-1" />
                        </Trigger>
                        <Trigger Property="IsLoading" Value="True">
                            <Setter TargetName="LoadingIndicator" Property="Visibility" Value="Visible" />
                            <Setter TargetName="ButtonContentPresenter" Property="Opacity" Value="0.9" />
                            <Setter Property="IsHitTestVisible" Value="False" />
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="LoadingSpinBeginStoryboard">
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation
                                            By="360"
                                            Storyboard.TargetName="LoadingRotateTransform"
                                            Storyboard.TargetProperty="Angle"
                                            Duration="0:0:0.8" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="LoadingSpinBeginStoryboard" />
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource NButtonBaseStyle}" TargetType="{x:Type controls:NButton}"/>
</ResourceDictionary>
